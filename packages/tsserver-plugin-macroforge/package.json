{
  "name": "@macroforge/tsserver-plugin-macroforge",
  "private": true,
  "version": "0.0.1",
  "description": "TypeScript language service plugin that augments classes decorated with @derive to include macro-generated methods.",
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "test": "bun run build --silent && node --test tests/**/*.test.js",
    "prepublishOnly": "bun run clean && bun run build"
  },
  "dependencies": {
    "macroforge": "file:../../crates/macroforge_ts"
  },
  "peerDependencies": {
    "typescript": ">=5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
