{
	"name": "@playground/macro",
	"version": "0.0.1",
	"private": true,
	"main": "index.js",
	"types": "index.d.ts",
	"napi": {
		"binaryName": "playground-macro",
		"triples": {}
	},
	"files": [
		"index.d.ts",
		"index.js"
	],
	"scripts": {
		"build": "bun x @napi-rs/cli build --platform --release -p playground-macros",
		"prepare": "bun run build",
		"clean": "rm -rf *.node",
		"cleanbuild": "rm -rf *.node && bun x @napi-rs/cli build --platform --release -p playground-macros"
	},
	"engines": {
		"node": ">= 18"
	}
}
