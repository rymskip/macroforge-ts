{
	"name": "@playground/macro",
	"version": "0.0.1",
	"private": true,
	"main": "index.js",
	"types": "index.d.ts",
	"napi": {
		"binaryName": "playground-macro",
		"triples": {}
	},
	"files": [
		"index.d.ts",
		"index.js"
	],
	"scripts": {
		"build": "npx -y -p @napi-rs/cli napi build --platform --release --package playground-macros",
		"prepare": "npm run build",
		"clean": "rm -rf *.node",
		"cleanbuild": "rm -rf *.node node_modules && npm install && npx -y -p @napi-rs/cli napi build --platform --release --package playground-macros"
	},
	"dependencies": {
		"macroforge": "file:../../crates/macroforge_ts"
	},
	"engines": {
		"node": ">= 18"
	}
}
